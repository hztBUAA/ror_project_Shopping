<h1>所有订单</h1>

<div id="orders">
  <% @orders.each do |order| %>
    <%= render order %>
    <p>
      <%= link_to "进入", customer_order_path(@customer,order),class: "btn btn-primary"%>
    </p>
  <% end %>
</div>

<%#= link_to "New order", new_customer_order_path(@customer,commodity_id: ) %>
